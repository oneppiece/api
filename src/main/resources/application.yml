server:
  servlet:
    context-path: /app
  port: 8080
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/angular
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  aop:
    proxy-target-class: true